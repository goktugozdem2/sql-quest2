# Search Console audit — 2026-09-13

Read in the browser, property `sc-domain:sqlquest.app`. The **Page indexing**
report's data is dated **2026-09-04** (Google's lag); URL Inspection is live.

## Coverage

| | |
|---|---|
| Indexed | **76** |
| Not indexed | 54 (5 reasons below) |
| Sitemap | submitted and last read 2026-09-12, 89 URLs discovered, Success |

| Reason | Pages | What they are | Action |
|---|---|---|---|
| Excluded by `noindex` | 32 | `/app/` and `/app.html` deep links (`?challenge=`, `?company=`, `?sector=`, `?topic=`, `?src=`) | Correct: the app shell is noindex by design. No action. |
| Page with redirect | 11 | `www.` and `http://` variants, `/fr/`, `/en/`, `/privacy.html`, `/refund.html` | Expected for www/http/locale. The `.html` legal URLs were **our own sitemap entries and links** — fixed: sitemap, canonicals and 141 links now use `/privacy/`, `/terms/`, `/refund/`. |
| Not found (404) | 8 | `/challenges/` (404 until 09-07), `/after-bootcamp/app.html`, `/{meta,amazon,google}-sql-interview/{terms,privacy,refund}.html` | All self-inflicted relative links. Fixed at the source (absolute `/app/?src=`, `/privacy/`), and `vercel.json` now 308s `/:slug/{privacy,terms,refund,app}.html` so the crawled URLs stop 404ing. Validate fix after deploy. |
| Crawled – currently not indexed | 2 | `/blog/sql-for-ai-company-interviews/`, `/tesla-sql-interview/` | Quality signal. Tesla is on the founder's first-20 list → rebuild on the company template (P0.5). The AI-company post gets internal links from the company pages (P1.9). |
| Redirect error | 1 | `/shopify-sql-interview` (no slash) | Validation started 2026-08-02; the live URL 308s once to `/shopify-sql-interview/` (checked). Leave. |

Also indexed but wrong: **`/terms.html` sits in the index beside `/terms/`** — the
canonical on the legal pages pointed at the `.html` form. Fixed; it drops out on
recrawl.

## Company, topic and question pages — are they on Google?

- **Company pages:** 22 of 23 in the 09-04 index list; the missing one,
  `/capital-one-sql-interview/`, inspects live as **"URL is on Google"** (so the
  report is just stale). `/tesla-sql-interview/` is crawled-not-indexed.
- **Topic pages** (`/challenges/*`, shipped 2026-09-07): **3 of 9 indexed**
  (joins, window-functions, cte). Not yet indexed: aggregation, case-when,
  date-functions, null-handling, string-functions, subqueries, and the
  `/challenges/` hub. Six days old; request indexing after the P1 rework.
- **Question pages:** none exist yet (P1.8).
- **Comparisons:** `/vs-sqlbolt/` and `/vs-sqlzoo/` not indexed; the other four
  and both hubs are.
- **Blog:** `/blog/validate-ai-generated-sql/` and
  `/blog/capital-one-codesignal-data-analyst-assessment/` not in the 09-04 list.

## Technical checks on the built site (`scripts/seo-audit.mjs`, guarded by `tests/seo-audit.test.js`)

Before: 9 errors, 202 warnings. After: **0 errors, 0 warnings** across 122 built
pages and 89 sitemap URLs — every internal link resolves from the page it sits
on, every sitemap URL is served directly (no redirects, no noindex), every
indexable page has a self-referencing canonical and a unique title, and
`/affiliate/` (a stale partner page outside the sitemap) is now `noindex`.

`robots.txt`: allows everything except `/api/` and the JS bundles; AI answer
engines named explicitly. No change needed.

## Core Web Vitals (Lighthouse desktop, local build)

| Template | Perf | SEO | LCP | CLS |
|---|---|---|---|---|
| `/` | 100 | — | 495 ms | 0.004 |
| `/stripe-sql-interview/` | 99 | 100 | 945 ms | 0.003 |
| `/challenges/joins/` | 99 | 100 | 730 ms | 0.012 |
| `/sql-exercises/` | 100 | 100 | 540 ms | 0.005 |
| `/blog/window-functions-tutorial/` | 100 | 100 | 451 ms | 0.000 |

Search Console's own Core Web Vitals report has too little field traffic to
rate URLs; re-read it when monthly clicks pass ~1,000.
